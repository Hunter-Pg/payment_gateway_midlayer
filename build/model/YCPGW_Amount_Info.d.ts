export declare class YCPGW_Amount_Info {
    private _total;
    private _currency;
    constructor(total: number, currency: string);
    get currency(): string;
    set currency(value: string);
    get total(): number;
    set total(value: number);
}
